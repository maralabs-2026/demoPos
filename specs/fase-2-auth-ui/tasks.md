# Tareas: Auth UI Fase 2

Contexto: sin Supabase Auth esta semana; sin Server Actions; sin tocar `supabase/`; sin dependencias nuevas; no modificar `spec.md`, `plan.md` ni `design/auth.pen`. El módulo `src/modules/auth/` ya existe como esqueleto (`schemas.ts`, `actions.ts`, `queries.ts`, `index.ts` vacíos).

- [ ] T1 Schemas Zod en `src/modules/auth/schemas.ts` (extender): `loginSchema` (correo recortado y minúsculas, obligatorio, válido, máx. 254; contraseña obligatoria, máx. 128, sin recortar) y `changePasswordSchema` (actual obligatoria, nueva + confirmación iguales, nueva min. 8 y máx. 128, distinta de la actual). Exportar por `index.ts`. Tests unitarios Vitest. Cubre R6, CA1. Hecho cuando: `npm run test` en verde.
- [ ] T2 Mock de datos en `src/modules/auth/mock.ts`: usuarios por rol (dueño, encargado, cajero) con email, contraseña, flag de contraseña temporal, nombre y comercio; marcado `// TODO(fase-2): reemplazar por Supabase Auth (semana 2)`. Exportar por `index.ts`. Cubre R5. Hecho cuando: el TODO está en el repo y el build pasa.
- [ ] T3 Componente `LoginForm` en `src/modules/auth/components/` (client): valida con `loginSchema`; errores de campo sin llamar al servidor; credenciales incorrectas muestran solo "Correo o contraseña incorrectos"; mensajes aparte para demasiados intentos, usuario desactivado y error de red/servidor; botón deshabilitado durante el envío (doble clic no duplica); Enter envía; foco inicial en correo; tras un fallo conserva el correo, limpia la contraseña y enfoca la contraseña. Usa las plantillas `Input` y `Button` de `src/components/ui/`. La autenticación mock vive en `mock.ts` como `authenticateWithMock` y respeta el formato estándar de acciones de CLAUDE.md: `{ ok: true, data } | { ok: false, error }`. Cubre R1, CA1, CA2, CA4. Hecho cuando: se comporta según la spec y coincide con el frame Login de `design/auth.pen`.
- [ ] T4 Página `/login` y validación segura de `next`: solo acepta rutas propias (empiezan con "/" y no con "//"), por defecto `/vender` (ignora `next=https://sitio-externo.com`); si el usuario tiene contraseña temporal redirige a `/cambiar-contrasena` antes que a cualquier pantalla; renderiza `LoginForm`. Cubre R1, CA3 y la regla de contraseña temporal. Hecho cuando: `npm run build` limpio y el caso de `next` externo se verifica en dev.
- [ ] T5 Cambio obligatorio de contraseña temporal: componente `ChangePasswordForm` + página `/cambiar-contrasena` (client); valida con `changePasswordSchema`; la vista le pasa la contraseña actual (temporal) desde la sesión para validar que la nueva sea distinta; errores genéricos; al guardar vuelve a `/vender`. Usa las plantillas `Input` y `Button` de `src/components/ui/` (incluye el toggle mostrar/ocultar como `Button` ghost de tamaño fijo para no alterar el ícono). La sesión guardada en `sessionStorage` (`MockSession`) NO incluye la contraseña: `storeMockSession` la excluye y `getMockSession` no la expone; para la validación "distinta de la actual" la vista resuelve la contraseña temporal desde el mock (`findMockUsuario(session.email)`) en el momento de cargar, sin persistirla. Equivale al frame Cambiar contraseña de `design/auth.pen`. Cubre R2. Hecho cuando: un usuario con contraseña temporal no accede a ninguna otra pantalla hasta cambiarla, no puede reutilizar la misma contraseña y su contraseña nunca queda guardada en `sessionStorage`.
- [ ] T6 Navegación por rol: componente `Navigation` (client) que muestra/esconde links según rol, integrado únicamente en las pantallas/layout autenticados (Vender, Productos y Perfil) respetando la estructura existente del proyecto; `/login` y `/cambiar-contrasena` no muestran la navegación autenticada. Permisos: dueño (Vender, Productos, Reportes, Config, Mi perfil), encargado y cajero (Vender, Productos, Mi perfil). Reportes/Config linkean a rutas futuras sin crear esas pantallas en esta fase. Cubre R3 y la tabla de roles. Hecho cuando: coincide con los frames de navegación de `design/auth.pen` en mobile y desktop.
- [ ] T7 Perfil de solo lectura: componente `Profile` + página `/perfil` (nombre, correo, rol, comercio) con botón "Cerrar sesión"; sin selector de comercio. Cubre R4. Hecho cuando: coincide con el frame Perfil de `design/auth.pen` en 360 px y desktop, y es solo lectura.
- [ ] T8 Verificación final responsive y de criterios: en `npm run dev` comparar `/login`, `/cambiar-contrasena`, `/perfil` y las 3 variantes de menú contra `design/auth.pen` en 360 px y escritorio (sin scroll horizontal); correr `npm run build`, `npm run lint` y `npm run test` en verde; recorrer los criterios de aceptación de la spec y tildar cada uno con evidencia. Cubre todos. Hecho cuando: checklist de la spec completo con evidencia. Corrección REV#5: la UI de auth usa tokens del tema de `globals.css` (`bg-primary`, `text-foreground`) en lugar de hex cuando existe equivalente exacto; los hex sin token equivalente (`#101D27`, `#474556`, `#5B6B7A`, `#4D627D`, `#4A607B`, `#707D8A`, `#9CA3AF`, `#C8DFFE`, `#E2EFFE`, `#ECF4FF`, `#F7F9FF`, `#3904E7`) quedan y se informan, sin inventar tokens nuevos.